# 📘 Sudoku

## 🔧 Instruções de Compilação e Execução:

1. Abra o terminal no diretório principal do projeto.  
2. Digite:

```bash
make
```

## ❓ O que o makefile faz:

O makefile criará um subdiretório "bin", onde serão armazendos os
arquivos ".class". Logo em seguida ele executa o programa.

O "make" pode ser executado várias vezes, pois a cada chamada ele
remove as classes antigas. 
